name: Test Backend

on:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Run pytest
        run: docker build -t nodejs_postgres-node:latest